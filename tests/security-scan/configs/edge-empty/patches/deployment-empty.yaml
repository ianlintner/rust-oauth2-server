apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-server
spec:
  template:
    spec:
      containers:
        - name: oauth2-server
          env:
            - name: OAUTH2_DATABASE_URL
              value: "sqlite:/tmp/oauth2.db?mode=rwc"
