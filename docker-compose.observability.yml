version: '3.8'

services:
  jaeger: # nosemgrep: yaml.docker-compose.security.writable-filesystem-service
    image: jaegertracing/all-in-one:latest
    container_name: oauth2_jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686" # Jaeger UI
      - "4317:4317"   # OTLP gRPC
    security_opt:
      - no-new-privileges:true
    # Jaeger writes span data to memory/tmp; read_only would require tmpfs mounts.
    restart: unless-stopped

  otel_collector: # nosemgrep: yaml.docker-compose.security.writable-filesystem-service
    image: otel/opentelemetry-collector:0.103.0
    container_name: oauth2_otel_collector
    command: ["--config=/etc/otelcol/config.yml"]
    volumes:
      - ./observability/otel/otel-collector.yml:/etc/otelcol/config.yml:ro
    ports:
      - "4318:4318" # OTLP HTTP (optional)
    security_opt:
      - no-new-privileges:true
    # OTel collector writes temporary pipeline buffers; read_only would require tmpfs mounts.
    depends_on:
      - jaeger
    restart: unless-stopped

  prometheus: # nosemgrep: yaml.docker-compose.security.writable-filesystem-service
    image: prom/prometheus:v2.55.0
    container_name: oauth2_prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./observability/prometheus/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    security_opt:
      - no-new-privileges:true
    # Prometheus writes TSDB data; persistence is constrained to the named volume above.
    restart: unless-stopped

  grafana: # nosemgrep: yaml.docker-compose.security.writable-filesystem-service
    image: grafana/grafana:11.3.0
    container_name: oauth2_grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "false"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./observability/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./observability/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    security_opt:
      - no-new-privileges:true
    # Grafana writes session/plugin data; persistence is constrained to the named volume above.
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  prometheus_data:
  grafana_data:
